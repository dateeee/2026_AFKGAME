# 次タスク開始 — プロジェクト固有プロファイル

> 一般手順は [.claude/skills/next/SKILL.md](../skills/next/SKILL.md) を参照。本書は AFK GAME 固有の値のみを持つ。
> 共通の値は [profile.md](profile.md)。

## 1. 引き継ぎファイル

| 項目 | 値 |
|------|-----|
| パス | `docs/backlog/next_session.md` |
| 構成 | §0 並行作業のルール + §1「次回（コピペ用）」1件 + §2 候補キュー最大5行（**ポインタ専用**。Phase 進捗は転記しない） |
| 更新タイミング | タスク完了のコミット前（Stop フック `stop-commit.sh`（`stop-chain.sh` 経由）がリマインドする）。worktree で作業した場合は **main へ統合してから main 側で1回**（同ファイル §0 ルール4） |
| 並行作業 | 着手中はファイルに書かず worktree の存在で示す。状態の確認は `python scripts/worktree.py list`（作業中 / 完了・未統合 / 空を判定して出す） |
| 申し送りの逃がし先 | 複数セッションにまたがるメモは `docs/backlog/carryover_notes.md`（`merge=union`・worktree からも追記可）。引き継ぎ側へ転記しない |
| 役割の宣言 | [docs/process/spec_ownership.md](../../docs/process/spec_ownership.md) §2 |

## 2. 開始プロンプトの定型（5要素）

```
/<工程スキル> <Phase・対象>: <作業内容を1行>
完了条件: <コミット・Red確認・C1 100% など>
参照: <ファイル1〜2点>（起点。追加はここから索引経由で）
前提: <直前の状態1行・コミットID>
```

設計の経緯と敵対的検証は [logs/session_start_prompt_design_2026-08-06.md](../../logs/session_start_prompt_design_2026-08-06.md)。

### 2.1 「前提」「完了条件」に環境の可用性を書くとき

外部ツール・ランタイム・ミドルウェア（JDK・Maven・docker・DBサーバー等）を書く場合のルール。

| # | ルール |
|---|-------|
| 1 | **書くターンで実際に実行し、コマンドと結果を根拠にする**。根拠なしに「確認済み」と書かない |
| 2 | **新しいシェルで再現するかまで確認する**。導入直後は PATH がユーザー環境変数へ未反映で、次セッションの新規シェルからは見えないことがある。PATH 依存ならフルパスを書く |
| 3 | 不在なら、代替手段まで含めて「前提」に明記する。**不在のまま完了条件へ書かない**（着手後に判明すると中断になる） |

## 3. 使いどころの制限

- `/clear` 後の**新セッション冒頭専用**。セッション途中の別タスク開始には使わない（[profile.md](profile.md) §6 規律6: 工程の区切りで /clear）
- 委譲先の工程スキル ↔ プロファイル対応は [INDEX.md](INDEX.md) の対応表を使う

## 4. 引き継ぎ更新時のチェック

引き継ぎを書き換えて作業を終える前に `git status --short` を確認する。
未コミットの書きかけ（別タスクへ切り出した成果物・試行の残骸）が残る場合は、
そのパスと状態を §1 の「次回」へ明記するか、破棄してから終える。

**引き継ぎに書かれていない未コミットファイルは次セッションの着手を止める**
（`??` の正体をユーザーに確認するまで作業を開始できない）。
