---
name: next
description: 次タスクの開始。引き継ぎファイルに記録された「次回開始プロンプト」を読み込み、鮮度を確認してから該当する工程スキルへ委譲する。新セッションの冒頭で「/next して」「次のタスクを開始して」など、タスク名を挙げずに作業を再開する依頼で使用する。引数で候補キューの番号・タスク名を指定できる。引き継ぎファイルのパス・定型はプロジェクト固有プロファイルから読み込む。
argument-hint: "[候補番号 | タスク名]"
---

# 次タスクの開始（next）

前セッションが書き残した引き継ぎから次のタスクを特定し、工程スキルへ委譲する。
**タスクを特定しないまま作業を始めないための入口**であり、本スキル自身は作業をしない。

## 0. 最初に読む

| 順 | ファイル | 内容 |
|----|---------|------|
| 1 | `.claude/project/profile.md` | コスト規律・ディレクトリ |
| 2 | `.claude/project/next.md` | 引き継ぎファイルのパス・開始プロンプトの定型・更新ルール |
| 3 | プロファイルが指す申し送りファイル | 既知の制約・環境・前セッションの発見。§1 の「他の仕様書・コードを読まない」の例外 |

## 1. タスクの特定

1. プロファイルが指す引き継ぎファイルを読む（**この時点では他の仕様書・コードを読まない**）
2. 引数なし → 「次回」ブロックを対象にする。引数あり → 候補キューの該当行を対象にし、プロファイルの定型に沿ったプロンプトへ組み立てる
3. 「次回」がユーザー判断待ちの場合 → 候補キューを提示して選んでもらう。**選ばれるまで作業を開始しない**
4. 対象が1セッションで閉じない規模（複数ファイル・複数領域）と分かったら、**着手前にセグメントへ割る**。先頭セグメントだけを今回の対象にし、残りは候補キューへ行として戻す

## 2. 鮮度の確認

「前提」のコミットIDと git log 最新5件程度を突合し、あわせて「前提」「完了条件」に挙がった外部ツール・ランタイムを**着手前に実行して確認する**。次のいずれかに該当したら**開始せず**、状況と選択肢を提示してユーザーの判断を仰ぐ。

| # | 開始を止める兆候 |
|---|---------------|
| 1 | 対象タスクが完了済みに見える（コミットメッセージ・成果物と一致する） |
| 2 | 「前提」のコミットより後に、対象領域へ別の大きな変更が入っている |
| 3 | 引き継ぎファイルの最終更新・対応コミットが git log と食い違う |
| 4 | 「前提」の外部ツール・ランタイムが実行できない（未導入、または PATH が新規シェルへ未反映） |

## 3. 工程スキルへの委譲

1. プロファイルが worktree 運用を定めている場合、**委譲の前に**その手順で worktree を作成しセッションを移す（読み取りのみのタスク、単独更新ファイルだけを触るタスクは移らない）
2. 「次回」プロンプトを**ユーザーの依頼文として扱い**、冒頭に書かれた工程スキルを起動する
3. 「参照」は読み始めの**起点**。追加で必要なファイルはそこから索引経由で特定する（全文読みをしない）
4. 以降の進め方・完了基準・報告は**委譲先スキルに従う**

## 4. 引き継ぎの更新（セッション終了時）

委譲先のタスクが完了したら、**コミット前に**引き継ぎファイルを更新する。
worktree で作業した場合はプロファイルの統合手順で main へ戻してから更新する（引き継ぎファイルは main でのみ更新する）。

- 「次回」を次のタスクの完成形プロンプト（定型5要素）へ書き換え、最終更新・対応コミットを更新する
- **候補キューの1行 = 1セッションで閉じる規模**。対象が複数ファイル・複数領域に及ぶタスクは、キューへ書く時点でセグメントへ割って行を分ける（1行にまとめない）
- 完了した行を候補キューへ残さない
- 次タスクを確定できない場合は「次回」を「ユーザー判断待ち」とし、候補キューに選択肢を残す

## 注意事項

- **サブエージェントを使わない**。読むのは引き継ぎファイルとプロファイルのみ
- 引き継ぎファイルに無いタスクを推測で開始しない
- 依頼文でタスクが特定されている場合は本スキルを経由せず、該当する工程スキルを直接使う
