# AFK GAME — 性能・容量設計

> [tech_spec.md](tech_spec.md) §10。**目標値（どこまで満たすか）は [non_functional_requirements.md](../design/non_functional_requirements.md) §1〜§3 が正**。本書はそれを満たすための**実現方式**を扱い、目標値を再掲しない。
> 運用面の監視・移行手順は [tech_operations.md](tech_operations.md)、tick処理そのものは [tech_tick.md](tech_tick.md)。変更履歴は親に集約（[§9](tech_spec.md#9-変更履歴)）。

---

## 10.1 負荷の前提と見積り

| 項目 | 見積り | 根拠 |
|------|--------|------|
| 1ユーザーのリクエスト | tick 1回/60秒 + 操作系 平均0.5回/分 | ハイブリッドtick制（[tech_architecture.md](tech_architecture.md) §7） |
| 同時アクティブ100人時のRPS | 約 2.5 req/s | 100 × (1/60 + 0.5/60) |
| オフライン復帰の集中 | 10 req/s（朝夕ピーク想定） | 復帰時tickは重いため別枠で見積もる |
| ユーザー間のロック競合 | **発生しない** | シングルプレイ専用。競合は同一ユーザーの多重タブのみ（[tech_tick.md](tech_tick.md) §3） |
| 全ユーザー横断の重いクエリ | ランキング取得のみ | `/api/boss-rush/ranking`・`/api/abyss/ranking` |

- スケール戦略は **垂直（1インスタンス）**。水平分割は想定規模に対して過剰なため行わない
- 水平分割を行う場合、レート制限カウンタ（[tech_security.md](tech_security.md) §11.6）が共有ストア（Redis 等）を必要とする点が制約になる

## 10.2 負荷の平滑化

| 項目 | 設計 |
|------|------|
| ポーリングの同時集中 | クライアントごとに **0〜5秒のランダムジッター**を初回ポーリングに付与し、tickリクエストの位相をばらす |
| リトライの集中 | 指数バックオフ（1/2/4秒）に **±20% のジッター**を加える（[tech_architecture.md](tech_architecture.md) エラーハンドリング） |
| 重い処理の扱い | 簡略計算（101tick以上）は**同期実行のまま**とする。復帰時に結果を画面へ返す必要があるため、非同期ジョブ化しない |
| DB書き込み | 1リクエスト内でtick単位の書き込みを行わず、メモリ上で集計して一括反映する（[tech_tick.md](tech_tick.md) §4） |

- tick処理時間が **60秒（ポーリング周期）を超えると次tickと重なる**ため、これを超えた場合は体感の遅さとは別に即時の対処対象とする

## 10.3 データ容量の見積り

保持上限そのものは [non_functional_requirements.md](../design/non_functional_requirements.md) §2 が正。本節はそこから導かれる**サイズ見積り**を示す。

| データ | 上限 | 見積りサイズ |
|--------|------|-------------|
| 戦闘ログ | 100件 | 約 20KB |
| 装備インベントリ | 50〜300枠（倉庫LV依存） | 約 45KB |
| キャラクター | 数十体（酒場スカウトで増加） | 約 10KB |
| アイテム・素材 | 種類数 × 所持数 | 約 5KB |
| プレイヤー本体・設定・塔進行 | 1行 | 約 3KB |
| リフレッシュトークン | 有効期限30日分 | 約 2KB |
| **合計** | | **約 85KB / 人** |

| 規模 | DBサイズ | 判断 |
|------|---------|------|
| 1,000人 | 約 85MB | SQLite で問題なし |
| 10,000人 | 約 850MB | **PostgreSQL 移行の判断ライン**（[tech_operations.md](tech_operations.md) §12.4） |

- **上限のないデータを新設しない**。新テーブルには必ず「1プレイヤーあたりの上限」または「保持期間」を定義し、上表と非機能要件 §2 に追記する
- APIレスポンスは戦闘ログ50件（`MAX_LOG_PER_RESPONSE`）を上限とし、ペイロードを概ね 100KB 以内に収める

## 10.4 性能劣化時の対処順序

目標を超過した場合、実装コストの低い順に適用する。

| 順 | 手段 | 副作用 |
|----|------|--------|
| 1 | `FAST_CALC_THRESHOLD` を 100 → 30 などに下げる | 個別戦闘ログが出ない範囲が広がる |
| 2 | ランキングを60秒キャッシュする | 順位反映が最大60秒遅れる |
| 3 | `MAX_LOG_PER_RESPONSE` を 50 → 20 に下げる | 復帰直後に見えるログが減る |
| 4 | tickレスポンスに `nextPollSeconds` を追加し、負荷時に60秒より長い値を返す | ポーリング頻度のみ低下。tick間隔は60秒固定のため**報酬は減らない** |
| 5 | SQLite → PostgreSQL 移行 | 書き込みロック競合の解消 |

- 手段4はAPI変更を伴うため**現時点では未実装**。設計上の逃げ道として明記しておく
- **ゲーム性に影響する調整（tick間隔の変更・報酬効率の低下）は性能対策として行わない**

## 10.5 性能検証方法

| 検証 | 内容 | 実施タイミング |
|------|------|--------------|
| 常時計測 | ミドルウェアの `duration_ms`（[tech_logging.md](tech_logging.md)）から p95 を確認 | 常時 |
| 最悪ケース検証 | 24時間放置相当（1,440tick）の復帰を実データで計測 | 結合テスト（Phase単位） |
| 負荷試験 | 同時100ユーザーのポーリングを再現（k6 または locust） | Phase 2 結合テストで初回実施 |
| DB容量検証 | 1,000人相当のダミーデータを投入しサイズとクエリ時間を確認 | 負荷試験と同時 |

- 検証は [development_process.md](../development_process.md) §3.7 結合テストの一部として扱い、Phase完了ゲートの判定材料にする

---

> 変更履歴は [tech_spec.md](tech_spec.md#9-変更履歴) を参照。
