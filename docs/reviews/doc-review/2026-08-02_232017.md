# 仕様レビュー結果 — 2026-08-02 23:20:17

> モード: 差分（前回 2026-08-02_225639 以降の変更: CLAUDE.md, README.md, docs/balance_backlog.md, docs/design/game_spec.md, docs/design/operation_requirements.md, docs/design/systems/endgame.md, docs/development_process.md, docs/open_specs.md, docs/spec_ownership.md, docs/tech/tech_auth.md）

## サマリー

| カテゴリ | 件数 |
|---------|------|
| 整合性 | 1件 |
| 網羅性 | 2件 |
| 規約 | 1件 |
| 合計 | 4件 |

重要度別: 高 0件 / 中 3件 / 低 1件。

**機械的検証（そのまま取り込み）**

| スクリプト | 結果 |
|-----------|------|
| `python scripts/check_doc_size.py --sections` | exit 0（130 files: 130 OK / 0 違反 / 残量WARN 10） |
| `python scripts/check_docs.py` | exit 0（リンク OK / 索引到達性 OK / 曖昧語 OK / 正の逸脱 OK） |

**前回指摘（ISSUE-701〜706）の反映確認**

| ISSUE | 反映状況 |
|-------|---------|
| 701 | 部分。指定3ファイル + `balance_backlog.md` は修正済みだが、**未修正が3ファイル残る**（ISSUE-802） |
| 702 | 済（`development_process.md:159`、`open_specs.md:6`） |
| 703 | 部分。台帳へ2件移管したが、**同じ先送りが `tech_api.md` に残る**（ISSUE-801） |
| 704 | 済（`tech_auth.md:58,173`。`tech_security.md` §11.7 / `tech_architecture.md` と整合） |
| 705 | 済（`README.md:60-61`。`scripts/*.py` の4本すべてが表に揃った） |
| 706 | 済（`spec_ownership.md:32` + 新規2行） |

---

## 指摘一覧

### ISSUE-801: ISSUE-703 で台帳へ移した決定先送りが `tech_api.md` に二重に残り、確定時に取り残される

| 項目 | 値 |
|------|-----|
| カテゴリ | 整合性 |
| 重要度 | 中 |
| 関連ファイル | docs/tech/tech_api.md, docs/open_specs.md |
| 該当箇所 | tech_api.md 行135・行139 / open_specs.md §2「決定時にすること」・§3「決定時にすること」 |
| 検出可能工程 | 本レビュー（前回レポート「プロセスへの還元」提案1のスクリプト化で機械検出可能） |

**問題の説明:**

ISSUE-703 は「決定先送り2件を `open_specs.md` へ移管する」指摘だったが、移管元として扱ったのは `endgame.md:119` と `operation_requirements.md:61` の2箇所だけで、**同じ決定を宣言している `tech_api.md` の2行が手つかず**で残っている。

```
tech_api.md:135  … 難易度別の到達記録を `towersCleared`（tech_data.md）へ保持する
                    キー体系の確定、および本節への追記は Phase 5 の基本設計で行う。
tech_api.md:139  … エンドポイントの定義と、既読状態のクライアント保持先の確定は
                    Phase 3 の基本設計で行う（マスターの項目定義は Phase 3 の詳細設計）。
```

これは2つの不整合を生んでいる。

1. **台帳が唯一の管理先になっていない**: 同じ未確定項目の宣言が `open_specs.md` と `tech_api.md` の両方に存在し、`tech_api.md` 側は台帳へリンクしていない。`spec_ownership.md:34` で「未確定仕様の管理ルールの正は `development_process.md`、台帳は `open_specs.md`」と宣言した直後の状態と食い違う。
2. **確定時に取り残される**: `open_specs.md` §2 の「決定時にすること」は `operation_requirements.md` §3.1 と `tech_state.md` の2ファイル、§3 は `tech_data.md` の1ファイルしか挙げていない。`tech_api.md` が入っていないため、Phase 3／Phase 5 で確定させたときに `tech_api.md:135,139` の「〜で行う」が古いまま残る。

**修正案:**

- `docs/tech/tech_api.md:135`: 末尾を「…および本節への追記は Phase 5 の基本設計で行う（キー体系の未確定は [open_specs.md](../open_specs.md) で管理）。」とし、台帳へリンクする。
- `docs/tech/tech_api.md:139`: 末尾を「…エンドポイントの定義は Phase 3 の基本設計で行う（既読状態のクライアント保持先の未確定は [open_specs.md](../open_specs.md) で管理）。」とし、「保持先の確定」の主語を台帳へ移す。
- `docs/open_specs.md` §2「決定時にすること」: 「`operation_requirements.md` §3.1 と [tech/tech_state.md](tech/tech_state.md) へ保持先を明記し」→「`operation_requirements.md` §3.1、[tech/tech_state.md](tech/tech_state.md)、[tech/tech_api.md](tech/tech_api.md)「お知らせ」節へ保持先を明記し」に変更する。
- `docs/open_specs.md` §3「決定時にすること」: 「`tech_data.md` のセーブデータ構造へキー体系を定義し」→「`tech_data.md` のセーブデータ構造へキー体系を定義し、[tech/tech_api.md](tech/tech_api.md)「イベントダンジョン」節へ反映し」に変更する。
- なお `endgame.md:155`（ステータス値・報酬テーブルを Phase 5 の詳細設計で `master_data.md` へ定義）と `operation_requirements.md:61` の「マスターの項目定義」は、**選択肢が未決なのではなく後工程で書くだけ**の通常のPhase進行であり、台帳登録は不要。台帳へ載せる基準は「仕様の選択肢が2つ以上あり、どれを採るか決まっていない」ものに限る。

---

### ISSUE-802: 「`open_specs.md` は不在／未確定ゼロ」の記述が3ファイルに残存（ISSUE-701 の3度目の取りこぼし）

| 項目 | 値 |
|------|-----|
| カテゴリ | 網羅性 |
| 重要度 | 中 |
| 関連ファイル | docs/known_issues.md, .claude/project/dev.md, .claude/project/requirements.md, docs/open_specs.md |
| 該当箇所 | known_issues.md 行12 / dev.md 行43・行45 / requirements.md 行16・行23・行70-71 |
| 検出可能工程 | 本レビュー（前回レポート「プロセスへの還元」提案2のスクリプト化で機械検出可能） |

**問題の説明:**

`docs/open_specs.md` は3項目を持って**実在する**が、以下は依然として不在と断定している。ISSUE-701 で4箇所を直した直後にもかかわらず、走査範囲が `docs/design/` `docs/tech/` `CLAUDE.md` に限られていたため取りこぼした。

```
docs/known_issues.md:12
  - **未確定仕様**は `open_specs.md`（現在は未確定ゼロのため不在）、…
.claude/project/dev.md:43
  | 9 | `docs/open_specs.md` | 未確定仕様の確認（**存在する場合のみ**。Phase 1〜5 は全確定済みのため現在は不在） |
.claude/project/requirements.md:16
  | 未確定管理 | `docs/open_specs.md` | 未確定仕様のチェックリスト（**全解消済みのため現在は不在**。新たに生じたら作成する） |
```

`dev.md` は実装スキルが読むプロファイルであり、「現在は不在」を前提に未確定仕様チェックを省略しうる。Phase 3 の実装時に open_specs #1・#2 を見落とす経路になる。

加えて、プロファイルは `open_specs.md` を**チェックリスト形式**として記述しているが、実ファイルは「一覧表 + 項目ごとの4行詳細ブロック」であり形式が一致しない。

```
.claude/project/dev.md:45        実装対象に未確定仕様（`[ ]`）が含まれる場合、…
.claude/project/requirements.md:23  全文（チェックリストのみで軽い）。不在なら未確定ゼロと判断する
.claude/project/requirements.md:70  `docs/open_specs.md` の該当項目を削除（`[x]` のまま残さない）
```

**修正案:**

- `docs/known_issues.md:12`: 「**未確定仕様**は [open_specs.md](open_specs.md)、**数値のみ調整待ち**は [balance_backlog.md](balance_backlog.md) に置き、本書には含めない」（不在の断定を削除）。
- `.claude/project/dev.md:43`: 「未確定仕様の確認（**存在する場合のみ**。不在＝未確定ゼロ）」に短縮する。
- `.claude/project/dev.md:45`: 「未確定仕様（`[ ]`）が含まれる場合」→「未確定項目が含まれる場合」。
- `.claude/project/requirements.md:16`: 「未確定仕様の一覧（**原則ゼロ**。生じたときだけ作成する）」。
- `.claude/project/requirements.md:23`: 「全文（一覧表 + 詳細ブロックで軽い）。不在なら未確定ゼロと判断する」。
- `.claude/project/requirements.md:70`: 「該当項目を削除（`[x]` のまま残さない）」→「該当行と詳細ブロックを削除する」。
- `.claude/project/requirements.md:23` の「不在なら未確定ゼロと判断する」という**条件文そのものは正しい**ため残す（不在を事実として断定している箇所だけが対象）。

---

### ISSUE-803: 4件目の未確定仕様（Phase 5 エンドコンテンツの導線）が設計図にのみ存在し、仕様書と台帳のどちらにも無い

| 項目 | 値 |
|------|-----|
| カテゴリ | 網羅性 |
| 重要度 | 中 |
| 関連ファイル | docs/open_specs.md, docs/design/systems/ui.md, diagrams/screen_transition/endgame.md, diagrams/screen_transition/main_nav.md |
| 該当箇所 | endgame.md 行67 / main_nav.md 行164 / open_specs.md 一覧 |
| 検出可能工程 | 本レビュー（ISSUE-801 と同じ検出パターンで機械検出可能。走査範囲に `diagrams/` を含める必要あり） |

**問題の説明:**

「Phase N の基本設計で確定する」形の決定先送りを全文検索したところ、`open_specs.md` に登録されていない項目がもう1件見つかった。しかも**設計図にしか書かれていない**。

```
diagrams/screen_transition/endgame.md:67
  - ボスラッシュ・イベントダンジョンへの**導線**（タブ追加かホーム内セクションか）は
    Phase 5 の基本設計で確定する（main_nav.md「Phase別タブ構成」）
diagrams/screen_transition/main_nav.md:164
  - ボスラッシュ・イベントダンジョンの導線（タブ追加かホーム内セクションか）は
    **Phase 5 の基本設計**で確定する（systems/endgame.md）
```

`spec_ownership.md:49` は「図はテキスト仕様の視覚化であり正にならない」と定めている。したがって現状、この未確定仕様には**正となる記述が存在しない**。図の2ファイルが相互にリンクし合っているだけで、`docs/design/systems/ui.md`（ナビゲーション構造の正）にも `docs/design/systems/endgame.md` にも記載がなく、`open_specs.md` にも無い。

「タブ追加かホーム内セクションか」は選択肢が2つ提示された未決事項であり、`open_specs.md` #1（モバイルナビ「その他」の対象タブ）と同種・同じ正ファイル（`ui.md`）に属する。

**修正案:**

- `docs/open_specs.md` 一覧へ1行追加する。

  ```
  | 4 | ボスラッシュ・イベントダンジョンへの導線 | [design/systems/ui.md](design/systems/ui.md) ナビゲーション構造 | Phase 5 の基本設計 |
  ```

- `docs/open_specs.md` へ §4 詳細ブロックを §1〜§3 と同じ4行形式で追加する。
  - 確定している範囲: ボスラッシュ・イベントダンジョンは Phase 5 で追加される（[design/systems/endgame.md](design/systems/endgame.md) §2.13）。タブ構成は Phase 4 から変更しない前提（`main_nav.md`「Phase別タブ構成」）
  - 未確定な範囲: 導線をタブ追加とするか、ホーム内セクションとするか
  - 背景: Phase 4 で7項目に達しており、タブ追加はモバイル5枠上限（#1）に直結する。#1 の「その他」対象タブの決定と連動する
  - 決定時にすること: `ui.md` ナビゲーション構造へ導線を明記し、`diagrams/screen_transition/` の2図を追随させ、本書の行を削除する
- `docs/design/systems/ui.md` ナビゲーション構造（行94付近、#1 の記述の直後）へ1行追加し、正の所在を仕様書側に置く。

  ```
  - Phase 5 のボスラッシュ・イベントダンジョンへの導線（タブ追加かホーム内セクションか）は [open_specs.md](../../open_specs.md) で管理する
  ```

- 図側（`endgame.md:67` / `main_nav.md:164`）の記述を台帳へのリンクに置き換える作業は `diagrams-review` の担当。本レビューでは仕様書・台帳側のみを対象とする。

---

### ISSUE-804: `development_process.md` の残量が28字となり、次の追記が物理的に不可能

| 項目 | 値 |
|------|-----|
| カテゴリ | 規約 |
| 重要度 | 低 |
| 関連ファイル | docs/development_process.md, .claude/project/review-docs.md |
| 該当箇所 | development_process.md 全体（§3 工程定義 3,682字） / review-docs.md 全体 |
| 検出可能工程 | `scripts/check_doc_size.py --sections`（残量WARN として検出済み） |

**問題の説明:**

`check_doc_size.py` の残量WARN のうち、次の2件は**追記余地が実質ゼロ**で、以降の `fix-specs` が同ファイルへ1行足すことすらできない。

```
WARN  docs/development_process.md: 7,972字（上限8,000字・残り28字）
WARN  .claude/project/review-docs.md: 4,956字（上限5,000字・残り44字）
```

`development_process.md` は前回レポート時点で残り49字、今回28字とさらに逼迫した。過去4回のレポートで圧縮を繰り返しており、圧縮での延命は限界に達している。§3 工程定義だけで3,682字（H2上限2,000字も超過）を占め、[documentation_rules.md](../../documentation_rules.md) §6 の「索引 + 個別ファイル」分割の条件を満たす。

上限**超過**ではないため重要度は低だが、仕様確定ゲートの収束作業そのものを止める要因になる。

**修正案:**

- `docs/development_process.md` を索引 + 個別ファイル構成へ分割する。
  - `docs/development_process.md`（索引）: §1 概要・§2 現況・工程一覧表・各個別ファイルへのリンク
  - `docs/process/phases.md`: §3 工程定義（3,682字。単独ファイルなら区分C 8,000字に十分収まる）
  - `docs/development_process.md` に残る §4 品質ゲート・§5 テスト方針・§6 変更管理はそのまま索引側に置く
  - 分割後は `README.md` 開発プロセス節、`CLAUDE.md` 開発方針、`.claude/project/profile.md` §1 のリンクを索引に向けたまま維持し、§3 を直接指していた参照（`spec_ownership.md:34` の「§6 が正」など節番号参照）は分割後も節番号を維持する
- `.claude/project/review-docs.md` は §2 全量モードの分担表・§4 `diagrams-review` の観点表がともに `diagrams-review` 専用のため、`diagrams-review` 固有分を別プロファイルへ切り出せる。ただし本レビューの担当外（スキル定義側の構成変更）のため、提案に留める。
- **この分割は大きな構造変更にあたるため、`fix-specs` では実施せず、ユーザーの判断を仰ぐこと。**

---

## 担当範囲外の言及

| 内容 | 担当スキル |
|------|----------|
| `docs/design/systems/ui.md:155` と `diagrams/screen_transition/main_nav.md:108` のログアウト表示条件（認証ユーザーのみ表示か）の突合 — 前回から未処理 | `diagrams-review` |
| ISSUE-803 の図側対応（`screen_transition/endgame.md:67`・`main_nav.md:164` を台帳リンクへ置換） | `diagrams-review` |
| ISSUE-704 で LocalStorage に確定したリフレッシュトークン保管が、実装（`frontend/src/api/client.ts`・認証ストア）と一致しているか | `full-review` |
| `AppHeader.vue` / `GameView.vue` / `navItems.ts` と `ui.md` ナビゲーション構造の突合 | `full-review` |

---

## プロセスへの還元

**検出可能工程の集計**

| 検出可能工程 | 件数 |
|------------|------|
| 本レビュー（機械検証で拾えていない） | 3件（801・802・803） |
| `check_doc_size.py`（検出済み・未対応） | 1件（804） |

**指摘が「本レビュー」に偏っている。**しかも3件とも、前回レポートの「プロセスへの還元」で提案済みだが未実装のチェックに該当する。提案が実装されていれば `doc-review` を回す前に検出できた。

| 前回提案 | 状態 | 今回の該当指摘 |
|---------|------|--------------|
| 提案1: 「Phase N の（基本設計\|詳細設計）で（確定\|決める\|定義する）」に台帳の行を要求 | 未実装 | ISSUE-801, ISSUE-803 |
| 提案2: `open_specs.md` の実在と本文の断定の突合 | 未実装 | ISSUE-802 |
| 提案3: `scripts/*.py` と README／profile のコマンド表の突合 | 未実装 | 今回は該当なし（ISSUE-705 で手作業解消済み） |

**実装案（`scripts/check_docs.py` へ追加）**

1. `--pending`（新規）: `docs/**` `diagrams/**` から正規表現 `Phase [0-9]+ の(基本設計|詳細設計)で.{0,40}(確定|決める|決定)` に一致する行を集め、その行が `open_specs.md` または `balance_backlog.md` へのリンクを含むか、または `open_specs.md` の一覧に対応する行があるかを検査する。どちらも無ければ ERROR。
   - 「定義する」「追記する」（＝選択肢が未決ではなく後工程で書くだけ）はパターンから除外し、ISSUE-801 修正案の基準と揃える。
   - 走査範囲に `diagrams/` を含めること（ISSUE-803 は `diagrams/` にしか無かった）。
2. `--ledger`（新規）: `docs/open_specs.md` の実在有無と、全 Markdown 中の「不在」「未確定ゼロ」「全解消済み」を含む `open_specs` 言及行との整合を検査する。実在するのに不在を断定する行、またはその逆を ERROR。
   - 走査範囲を `docs/**` `diagrams/**` に加えて **`CLAUDE.md` `README.md` `.claude/**`** まで広げる。ISSUE-802 の3件はすべて現行の走査範囲外にあった。
3. `--pending` の逆方向として、`open_specs.md` の各項目の「決定時にすること」に列挙されたファイルが、その項目に言及している全ファイルを網羅しているかを検査できるとなお良い（ISSUE-801 の後半＝確定時の取り残しを防ぐ）。

**`fix-specs` の運用ルールへの追加**

前回追加した「新規ファイルを作成した指摘は、そのファイルの『不在』を前提にした記述の全文検索を反映手順に含める」は実施されたが、**検索範囲が `docs/design/` `docs/tech/` `CLAUDE.md` に限られていた**ため ISSUE-802 を取りこぼした。ルールを次のように具体化する。

> 全文検索の範囲は `docs/**`（`reviews/` を除く）・`diagrams/**`・`CLAUDE.md`・`README.md`・`.claude/**` とする。プロファイル（`.claude/project/**`）は他スキルの判断根拠になるため、仕様書と同じ優先度で更新する。

また、ISSUE-801 は「レポートが挙げた移管元だけを直し、同じ宣言をしている第三のファイルを探さなかった」ことで生じた。`review-docs.md` §6 のルールへ次を追加する。

> 11. 記述を別ファイルへ**移管**する指摘は、移管対象のトピックを全文検索し、レポートが挙げていない同種の記述も同時に処理する。移管先が台帳（`open_specs.md` 等）の場合、台帳の「決定時にすること」へ**言及していた全ファイル**を列挙する。
