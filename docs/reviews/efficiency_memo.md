# 効率メモ（efficiency memo）

非効率だったやり取りの記録。追記型アーカイブ（文字数上限・doc検査の対象外）。
運用の正は [.claude/project/retro.md](../../.claude/project/retro.md)。

- **自動追記**: Stop フック `.claude/hooks/efficiency_check.py` が直近ターンの
  非効率シグナル（同一Readの再読・同一コマンドの連発・エラー多発・許可拒否・
  過大なツール呼び出し・手戻り発話）を検出すると仮エントリを追記し、
  Claude が「原因と改善案」を記入して完成させる
- **手動追記**: ユーザー・Claude が下の書式で直接追記してよい
  （「今のやり取りは非効率だったのでメモして」等）
- **反映**: `/retro` スキルがエントリを読み、スキル・プロファイル・成果物の改善へ
  反映する。**反映済み・対応不要のエントリは削除する**（open_specs と同じ運用。履歴は Git が持つ）

## エントリ書式

    ## YYYY-MM-DD HH:MM | session xxxxxxxx | 自動検出 or 手動
    - シグナル: <検出内容 or 状況の一言>
    - ターン概要: ツールN回・エラーN回・拒否N回。開始:「<プロンプト冒頭60字>」
    - 原因と改善案: <原因 + どのスキル/プロファイル/成果物をどう直すか（1〜2行）>

---

## 2026-08-06 23:21 | session f00467ad | 自動検出（**2026-08-07 の retro で見送り**）
- シグナル: same-command('cd c:/GIT/2026_AFKGAME && pyth'×3)
- 原因と改善案: `check_doc_size.py` の3回反復は、profile.md 痩身（4,995→4,482字）で削減量を見積もれず「削って測る」を繰り返したのが原因。`scripts/check_doc_size.py` の `--sections` は現状パス引数を無視して全量を出すため、単一ファイル指定でH2別文字数を出す経路を足せば1回で削減対象を決められる
- 見送り理由: スクリプト改修＋動作確認を伴うため、ドキュメント改稿のみの回とは分けて着手する

## 2026-08-06 23:40 | session c74cf804 | 手動（観測依頼・効果測定）

- シグナル: `review_prep.py` 導入（スキルリファクタ Stage 3）の効果を、**導入後最初のレビュー実行**で観測する
- 観測項目: (1) モード判定〜雛形生成に要したツール呼び出し回数（導入前は git 2〜3回 + レポート走査 + 採番の目視で概ね5〜8回）、(2) ISSUE の誤採番の有無、(3) `BASE` が「タイムスタンプで代替」のままか（HEAD SHA 記録済みレポートへ移行できたか）
- 原因と改善案: **本エントリは観測待ち**。最初のレビュー実行後に結果を追記し、`/retro` で評価して削除する。呼び出し回数が減っていない、または誤採番が出た場合は `.claude/scripts/review_prep.py` と `review-procedure.md` §2 を見直す

## 2026-08-06 23:39 | session c74cf804 | 自動検出（**2026-08-07 の retro で見送り**）
- シグナル: long-turn(calls=76)
- 原因と改善案: 呼び出しの約1/4は `review_prep.py` の手動検証（初回/差分/--full/壊れたSHA/変更0件/staged/untracked/非ASCII の8ケースを bash で都度組み立て）に費やされた。`.claude/scripts/` 配下のスクリプトに pytest を用意する規約を `_TEMPLATE.md`「規約: スクリプトの配置」へ足せば、次回以降は1コマンドで回帰確認できる（実際この検証で porcelain 先頭列の欠落バグを検出したため、検証自体は必要だった）
- 見送り理由: `.claude/scripts/` は現状 `review_prep.py` の1本のみ。2本目を追加するタイミングで規約化する

## 2026-08-07 10:51 | session abf953ac | 自動検出
- シグナル: long-turn(calls=30)
- ターン概要: ツール30回・エラー0回・拒否0回。開始:「<command-message>retro</command-message>」
- 原因と改善案: 誤検出: `/retro` が9エントリを分類し7件を7ファイルへ適用した回で、呼び出しの内訳は改稿対象の特定7回・編集8回・検査3回とタスク規模に比例している。むしろ長ターンを避けて分割すると承認の往復が増える性質の作業。しきい値変更は不要

## 2026-08-07 13:39 | session 103c7cce | 自動検出
- シグナル: long-turn(calls=69)
- ターン概要: ツール69回・エラー2回・拒否0回。開始:「<command-message>next</command-message>」
- 原因と改善案: 呼び出しの約1/3は使い捨て検証スクリプト2本（三者一致・Mermaid/リンク）の作成と2回ずつの修正で、三者一致は**3セグメント連続で書き直し**、しかも毎回同じ落とし穴（①定義書の「未実装」列を models 比較から除外する ②ER図が分割構成のためリレーション両端を全ファイル横断で解決する）を踏んで再実行した。候補キュー4の `scripts/check_schema_triple.py` 常設化を前倒しし、この2点を仕様に含める。あわせて `basic-design.md` §4 の機械検証表へ「未実装列の除外」「分割ER図では両端を全図から探す」を注記すれば、常設化前でも初回で通る
