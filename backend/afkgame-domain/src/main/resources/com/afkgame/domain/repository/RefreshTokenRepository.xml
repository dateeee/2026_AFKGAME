<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.afkgame.domain.repository.RefreshTokenRepository">

  <resultMap id="refreshTokenResultMap" type="com.afkgame.domain.model.RefreshToken">
    <id     property="id"        column="id"/>
    <result property="userId"    column="user_id"/>
    <result property="tokenHash" column="token_hash"/>
    <result property="expiresAt" column="expires_at"/>
    <result property="revoked"   column="revoked"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <select id="findByTokenHash" resultMap="refreshTokenResultMap">
    SELECT id, user_id, token_hash, expires_at, revoked, created_at
      FROM refresh_tokens
     WHERE token_hash = #{tokenHash}
  </select>

  <insert id="save">
    INSERT INTO refresh_tokens (user_id, token_hash, expires_at, revoked, created_at)
    VALUES (#{userId}, #{tokenHash}, #{expiresAt}, #{revoked}, #{createdAt})
  </insert>

  <update id="updateRevokedById">
    UPDATE refresh_tokens
       SET revoked = TRUE
     WHERE id = #{id}
  </update>

  <update id="updateRevokedByUserId">
    UPDATE refresh_tokens
       SET revoked = TRUE
     WHERE user_id = #{userId}
       AND revoked = FALSE
  </update>

</mapper>
